import "package:flutter/material.dart";

class ProfessorHome extends StatelessWidget {
  const ProfessorHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Professor Home"));
  }
}
